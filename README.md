# WebCam Controller

Windows PC의 웹캠과 마이크를 제어하고, 카메라 사용 여부를 실시간으로 감시하여 **텔레그램 · Discord 알림**을 보내는 보안 유틸리티입니다.

---

## 주요 기능

| 기능 | 설명 |
|------|------|
| 📷 카메라 제어 | 연결된 카메라 목록 표시, 클릭 한 번으로 활성화/비활성화 |
| 🎤 마이크 제어 | 연결된 마이크 목록 표시, 클릭 한 번으로 음소거/해제 |
| 👁️ 웹캠 감시 | 1~10초 간격으로 카메라 상태 자동 감시 (기본 3초) |
| 📱 텔레그램 알림 | 이상 감지 시 스마트폰으로 즉시 알림 (6가지 이벤트) |
| 🔔 Discord 알림 | 웹훅 URL 입력만으로 Discord 채널에 알림 전송 |
| 🎬 화면 녹화 | 웹캠 사용 감지 시 자동 녹화 또는 수동 녹화 시작 (MJPEG AVI) |
| 📋 이벤트 로그 | 날짜별 텍스트 파일에 이벤트 자동 기록, 앱 내 뷰어 제공 |
| 🖥️ 시스템 트레이 | 백그라운드 상주, 트레이 아이콘으로 조용히 동작 |
| 🔒 보안 저장 | 토큰 · URL을 Windows DPAPI로 암호화 저장 |
| 🛡️ 파일 변조 감지 | 실행 시마다 SHA-256 해시 검증 → 변조 시 즉시 알림 |
| ⚙️ 시작 프로그램 | Windows 로그인 시 관리자 권한으로 자동 실행 |

---

## 감시 알림 이벤트

| 이벤트 | 알림 메시지 |
|--------|------------|
| 감시 시작 | 🟢 웹캠 감시 시작 |
| 카메라 장치 활성화 | ⚠️ 웹캠 활성화 감지! |
| 카메라 장치 비활성화 | 🔴 웹캠 비활성화 감지! |
| 앱이 카메라 사용 시작 (Zoom, Teams 등) | 📷 웹캠 사용 감지! |
| 앱이 카메라 사용 종료 | ⏹️ 웹캠 사용 종료 |
| 감시 종료 / 프로그램 종료 | 🔴 WebCam Monitor 종료 |

---

## 요구사항

- Windows 10 / 11 (x64)
- 관리자 권한 (카메라 장치 제어에 필요)
- 인터넷 연결 (텔레그램 알림 사용 시)

---

## 설치

[Releases](../../releases) 페이지에서 `WebCamControl_Setup_v1.05.exe`를 다운로드하여 실행합니다.

설치 중 옵션:
- **Windows 시작 시 자동으로 실행** — 로그인 시 트레이에서 자동 감시 시작 (권장)
- **바탕화면에 바로가기 만들기**

> 설치 후 처음 실행 시 관리자 권한 요청 창이 표시됩니다. **예**를 눌러 허용해 주세요.

---

## UI 화면

### 메인 화면

```
┌─────────────────────────────┐
│  WebCam Controller          │
├─────────────────────────────┤
│ 카메라: [드롭다운         ] │
│ [      비디오 On/Off      ] │  ← 파란색: 켜짐 / 빨간색: 꺼짐
│                             │
│ 마이크: [드롭다운         ] │
│ [      마이크 On/Off      ] │  ← 파란색: 켜짐 / 빨간색: 음소거
│                             │
│ [          종료           ] │
├─────────────────────────────┤
│ ──── 웹캠 감시 ────         │
│ [감시시작][설정][알림 설정] │
│ 상태: 꺼짐                  │
│ ──── 화면 녹화 ────         │
│ [      녹화 시작           ]│
│ 상태: 꺼짐                  │
├─────────────────────────────┤
│ 제작자: jaeho  v1.05 도움말 │
└─────────────────────────────┘
```

### 설정 화면 — 일반 탭

```
┌─────────────────────────────────┐
│  설정  [ 일반 ] [ 화면 녹화 ]    │
├─────────────────────────────────┤
│ ── 감시 ──────────────────────  │
│ 감시 주기   [ 3 ▲▼ ] 초 (1~10) │
│                                 │
│ ── 알림 ──────────────────────  │
│ ☑ 트레이 풍선 알림 표시          │
│ ☑ 감시 시작·종료 알림 전송       │
│                                 │
│ ── 로그 ──────────────────────  │
│ ☑ 날짜별 로그 저장               │
│ C:\...\AppData\Local\...\logs   │
│ [폴더 열기]  [오늘 로그 보기]   │
├─────────────────────────────────┤
│        [확인]       [취소]       │
└─────────────────────────────────┘
```

### 설정 화면 — 화면 녹화 탭

```
┌─────────────────────────────────┐
│  설정  [ 일반 ] [ 화면 녹화 ]    │
├─────────────────────────────────┤
│ ── 화면 녹화 ─────────────────  │
│ ☑ 웹캠 감지 시 화면 자동 녹화    │
│ 녹화 대상: [주 모니터 (1920×1080)]│
│ 화질: ○낮음(5fps) ●보통 ○높음  │
│                                 │
│ ── 저장 위치 ─────────────────  │
│ C:\...\AppData\Local\...\rec... │
│ [폴더 열기]                     │
├─────────────────────────────────┤
│        [확인]       [취소]       │
└─────────────────────────────────┘
```

### 알림 설정 화면 — 텔레그램 탭

```
┌─────────────────────────────────┐
│  알림 설정                       │
├─────────────────────────────────┤
│ [  텔레그램  ] [  디스코드  ]    │
├─────────────────────────────────┤
│ ☑ 텔레그램 알림 활성화           │
│                                 │
│ Bot Token                       │
│ [________________________________]│
│                                 │
│ Chat ID                         │
│ [__________________________] 👁 │
│                                 │
│              [테스트 전송]       │
├─────────────────────────────────┤
│        [확인]       [취소]       │
└─────────────────────────────────┘
```

### 알림 설정 화면 — 디스코드 탭

```
┌─────────────────────────────────┐
│  알림 설정                       │
├─────────────────────────────────┤
│ [  텔레그램  ] [  디스코드  ]    │
├─────────────────────────────────┤
│ ☑ Discord 알림 활성화            │
│                                 │
│ Webhook URL (Discord 채널 설정에서 발급):│
│ [________________________________]│
│                                 │
│              [테스트 전송]       │
├─────────────────────────────────┤
│        [확인]       [취소]       │
└─────────────────────────────────┘
```

---

## Discord 웹훅 준비

1. Discord에서 알림을 받을 채널의 **채널 설정(⚙)** 클릭
2. **연동** 탭 → **웹후크** → **새 웹후크** 생성
3. **웹후크 URL 복사**
4. 앱에서 **알림 설정** → **디스코드** 탭 → URL 붙여넣기
5. **테스트 전송**으로 확인 후 **확인** 버튼 저장

---

## 텔레그램 봇 준비

### 1. Bot Token 발급
1. 텔레그램에서 `@BotFather` 검색 → 채팅 시작
2. `/newbot` 입력 → 봇 이름 설정
3. 발급된 **Bot Token** 복사 (예: `123456789:AABBcc...`)

### 2. Chat ID 확인
1. 텔레그램에서 `@userinfobot` 검색 → `/start` 입력
2. 표시된 **Your ID** 숫자 복사

### 3. 앱에서 설정
1. **알림 설정** 버튼 클릭 → **텔레그램** 탭
2. Bot Token · Chat ID 입력
3. **테스트 전송**으로 알림 확인
4. **확인** 버튼으로 저장

---

## 사용 방법

1. 텔레그램 설정 완료 후 **감시 시작** 버튼 클릭
2. 버튼이 빨간색 **"감시 중지"** 로 바뀌면 감시 시작
3. 창을 닫으면 트레이로 숨어서 계속 동작
4. 트레이 아이콘 더블클릭 → 창 복원 / 우클릭 → 메뉴

---

## 보안 기능

### DPAPI 암호화
Bot Token · Chat ID · Discord Webhook URL은 Windows DPAPI(현재 사용자 범위)로 암호화하여 저장합니다.
동일 PC · 동일 계정 외에는 복호화가 불가능합니다.

### 파일 무결성 검증
실행할 때마다 exe 파일의 SHA-256 해시를 HKLM 레지스트리 저장값과 비교합니다.
불일치(변조) 감지 시 텔레그램 알림 전송 후 프로그램을 종료합니다.

---

## 빌드

```powershell
# 개발 빌드
dotnet build

# 릴리즈 빌드 (프레임워크 의존, 단일 파일 — .NET 8 설치 필요)
dotnet publish -c Release -r win-x64 --self-contained false `
  -p:PublishSingleFile=true

# 인스톨러 (Inno Setup 7 필요)
# .NET 8 미설치 환경에서는 설치 시 자동으로 다운로드·설치
& "C:\Program Files\Inno Setup 7\ISCC.exe" WebCamControl.iss
```

---

## 기술 스택

| 항목 | 내용 |
|------|------|
| 언어 | C# (.NET 8, WinForms) |
| 카메라 제어 | Windows SetupAPI (P/Invoke) |
| 마이크 제어 | Windows CoreAudio API (P/Invoke) |
| 카메라 사용 감지 | Windows Registry (CapabilityAccessManager) |
| 화면 녹화 | GDI CopyFromScreen + SharpAvi (MJPEG AVI) |
| 텔레그램 알림 | Telegram Bot API (HttpClient) |
| Discord 알림 | Discord Webhook (HttpClient) |
| 이벤트 로그 | 날짜별 텍스트 파일 (AppData\Local\WebCamControl\logs) |
| 설정 암호화 | Windows DPAPI (ProtectedData) |
| 파일 무결성 검증 | SHA-256 + DPAPI (HKLM 레지스트리) |
| 시작 프로그램 등록 | Windows Task Scheduler (schtasks) |
| 인스톨러 | Inno Setup 7 |

---

## 향후 계획

- **로그 보관 기간 설정**: 오래된 로그 파일 자동 삭제 옵션 추가 예정
- **녹화 포맷**: MP4(H.264) 지원 검토 예정

---

## 라이선스

개인 사용 목적으로 제작되었습니다.

---

제작자: jaeho · jaeho9697@gmail.com
